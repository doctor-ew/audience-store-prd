#!/usr/bin/env bash
set -e
echo
echo "Welcome! Next steps:"
echo "  1) Run: gemini   (log in once if prompted)"
echo "  2) Ask Gemini to draft a spec from your PRD (features, acceptance criteria, guardrails)."
echo "  3) Run: pnpm install   (when you want to pull deps)"
echo "  4) Run: pnpm dev       (start the app when scripts exist)"
echo
echo "Optional:"
echo "  - Use Serena MCP (already wired)."
echo "  - Try Bun speed demo: bun install && bun run dev"
echo
